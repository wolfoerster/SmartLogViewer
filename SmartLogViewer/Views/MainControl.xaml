<UserControl x:Class="SmartLogViewer.Views.MainControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:SmartLogViewer.Views"
        xmlns:viewmos="clr-namespace:SmartLogViewer.ViewModels"
        mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance Type=viewmos:MainViewModel}"
        Background="{Binding ColorTheme.Background}"
        TextElement.FontSize="14">
  <UserControl.Resources>
    <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
      <Setter Property="Background" Value="{Binding ColorTheme.Background}"/>
      <Setter Property="Foreground" Value="{Binding ColorTheme.Foreground}"/>
    </Style>
    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{Binding ColorTheme.Background}"/>
      <Setter Property="Foreground" Value="{Binding ColorTheme.Foreground}"/>
      <Setter Property="Width" Value="80"/>
    </Style>
    <Style TargetType="ListBox" BasedOn="{StaticResource {x:Type ListBox}}">
      <Setter Property="Background" Value="{Binding ColorTheme.Background}"/>
      <Setter Property="Foreground" Value="{Binding ColorTheme.Foreground}"/>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
      <Setter Property="Background" Value="{Binding ColorTheme.Background}"/>
      <Setter Property="Foreground" Value="{Binding ColorTheme.Foreground}"/>
    </Style>
    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
      <Setter Property="Background" Value="{Binding ColorTheme.Background}"/>
      <Setter Property="Foreground" Value="{Binding ColorTheme.Foreground}"/>
    </Style>
  </UserControl.Resources>
  <Grid Margin="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <!--<StackPanel Orientation="Horizontal">
      <CheckBox Content="Dark mode" IsChecked="{Binding IsDarkMode}" Margin="12,0,0,0" VerticalAlignment="Center"/>
    </StackPanel>-->
    <GroupBox Grid.Row="1" Grid.Column="0" Header="Workspaces" Padding="8,0" MinWidth="120" MinHeight="300" Margin="4">
      <DataGrid Background="{Binding ColorTheme.Background}" Padding="4"
        ItemsSource="{Binding WorkspaceCollection}" 
        SelectedIndex="{Binding SelectedWorkspaceIndex}"
        AutoGenerateColumns="False"
        HeadersVisibility="None"
        Margin="0,8">
        <DataGrid.Columns>
          <DataGridTemplateColumn>
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Name}" 
                  Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  Foreground="{Binding DataContext.ColorTheme.Foreground, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  MinWidth="120"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
            <DataGridTemplateColumn.CellEditingTemplate>
              <DataTemplate>
                <TextBox Text="{Binding Name}" 
                  Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  Foreground="{Binding DataContext.ColorTheme.Foreground, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  MinWidth="120"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellEditingTemplate>
          </DataGridTemplateColumn>
          <!--<DataGridTemplateColumn>
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Grid Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}">
                  <Button Width="14" Height="14" Click="OnRemoveWorkspace" 
                    Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}"/>
                </Grid>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>-->
        </DataGrid.Columns>
      </DataGrid>
    </GroupBox>
    <GroupBox Grid.Row="1" Grid.Column="1" Header="Files" Padding="8,0" MinWidth="120" MinHeight="300" Margin="4">
      <!--<DataGrid Background="{Binding ColorTheme.Background}" Padding="4"
        ItemsSource="{Binding SelectedWorkspace.FileCollection}" 
        SelectedIndex="{Binding SelectedWorkspace.SelectedFileIndex}"
        AutoGenerateColumns="False"
        HeadersVisibility="None"
        Margin="0,8">
        <DataGrid.Columns>
          <DataGridTemplateColumn>
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}" 
                  Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  Foreground="{Binding DataContext.ColorTheme.Foreground, RelativeSource={RelativeSource AncestorType=local:MainControl}}" 
                  MinWidth="120"/>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTemplateColumn>
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Grid Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}">
                  <Button Width="14" Height="14" Click="OnRemoveFile" 
                    Background="{Binding DataContext.ColorTheme.Background, RelativeSource={RelativeSource AncestorType=local:MainControl}}"/>
                </Grid>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>-->
    </GroupBox>
  </Grid>
</UserControl>
